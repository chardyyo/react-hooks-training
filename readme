Branches:

main = module #6 (Hooks) & module #7 (Redux)


Running the app (locally) for frontend:

cd frontend/netflixroulette
yarn
yarn dev

Running the app (locally) for API/backend:

cd MoviesAPI.ReactJS
npm install
npm run watch